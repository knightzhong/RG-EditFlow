# Artifact Policy

This repository snapshot is code-only. Generated outputs are deliberately excluded:

- experiment metrics and CSV tables;
- figures and PDFs;
- checkpoint weights;
- raw logs and GPU telemetry;
- planning files and private notes.

Use the scripts in `scripts/` to regenerate outputs locally.
